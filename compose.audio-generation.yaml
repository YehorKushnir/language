services:
  api:
    environment:
      GOOGLE_APPLICATION_CREDENTIALS: /run/secrets/google-tts
    secrets:
      - google-tts

secrets:
  google-tts:
    file: ${GOOGLE_TTS_CREDENTIALS_FILE:?GOOGLE_TTS_CREDENTIALS_FILE is required for audio generation}
